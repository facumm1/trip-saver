/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import TripSaver from './src/TripSaver';

AppRegistry.registerComponent(appName, () => TripSaver);
